const colors = {
    primary: '#2567B4',
    secondary: '#F9B531'
}

const fonts = {
    primary: "'Raleway',sans-serif",
    secondary: "'Roboto', sans-serif"
}

export const theme = {
    colors,
    fonts
}

export type ThemeType = typeof theme